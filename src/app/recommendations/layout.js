export const metadata = {
  title: 'Rozgaar360 | Recommended Workers',
  description: 'Find the best skilled workers near you'
};

export default function RecommendationsLayout({ children }) {
  return children;
}